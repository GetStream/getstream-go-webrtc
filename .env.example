# Stream Video credentials for the examples.
# Create an app at https://dashboard.getstream.io and copy the key and secret.
# Never commit the filled-in file.

STREAM_API_KEY=
STREAM_API_SECRET=
# STREAM_USER_TOKEN=
STREAM_CALL_ID=my-call
# STREAM_USER_ID=go-subscriber
# STREAM_CALL_TYPE=default
